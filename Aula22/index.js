const corUsuario = 'vermelhoo'; //Cor escolhido pelo usuário
const corPadrao = corUsuario || 'preto'; //Se o usuário não escolher a cor(corUsuario),então usará a cor padrão preta
console.log(corPadrao);