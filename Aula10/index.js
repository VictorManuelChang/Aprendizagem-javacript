const nome = 'Luiz';//String
const nome1 = "Luiz";//String
const nome2 = `Luiz`;
const num1 = 10; //Number
const num2 = 10.52 //Number
let nomeAluno; //undefined -> não aponta para o local da memória
const sobrenomeAluno = null; // nulo -> não aponta para o local da memório, o programador que define null
const aprovado = false; //Boolean -> true, false

console.log(typeof sobrenomeAluno);