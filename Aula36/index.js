// const frutas = ['uva', 'maça', 'melancia'];

// for(let i in frutas) {
//     console.log(frutas[i]);
// }

const pessoa = {
    nome: 'Luiz',
    sobrenome: 'Otavio',
    idade: 12
}

// for(let key in pessoa) {
//     console.log(key);
// }

for(let key in pessoa) {
    console.log(key, pessoa[key]);
}